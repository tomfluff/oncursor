# Optional: endpoint of the deployed chart-analysis proxy (backend/).
# When set, the "Upload" button appears and users can analyze their own charts.
# When unset, the app is fully static (bundled example charts only).
# VITE_ANALYZE_URL=https://your-cloud-run-service-url/analyze
